"""
Reference models assembled from the layers in :mod:`rotorch.nn`, grouped by architecture.

Each model reproduces one of the experiments of the paper its architecture comes from, so
they double as worked examples of how the layers fit together. So far that is
:mod:`rotorch.models.cgenn`; GATr and others are to follow.
"""
